version: '3.8'

services:
  symfony:
    image: jabsraighs/ulysse_symfony_alpine
    volumes:
      - ./symfonyDockerCompose:/symfonyProject
    ports:
      - "8000:8000"
    depends_on:
      - postgres
    networks:
      - backend

  composer:
    image: jabsraighs/ulysse_composer_alpine
    volumes:
      - ./symfony/:/volume/symfonyProject
    networks:
      - backend

networks:
  backend:
    external: true
